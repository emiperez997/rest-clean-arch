{
  "name": "rest-clean-arch",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/app.ts",
    "build": "rimraf ./dist && tsc",
    "start": "bun run build && bun dist/app.js"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "rimraf": "^6.0.1",
    "ts-node-dev": "^2.0.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@types/express": "^4.17.21",
    "express": "^4.21.0"
  }
}